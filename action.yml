name: "Hello World JS Action"
description: "Greet someone and record the time"

inputs:
    who-to-greet:
        description: "Who to greet"
        required: true
        default: "World"

outputs:
    time:
        description: "The time we greeted you"

runs:
    using: docker
    image: docker://unitedbycode/alfredo:sha-c780c4b
    #image: Dockerfile
